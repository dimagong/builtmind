export interface IPosts{
    body: string,
    id: number,
    title: string,
    userId: number,
}

export interface IPhotos{
    albumId: number,
    id: number,
    title: string,
    url: string,
    thumbnailUrl: string,
}